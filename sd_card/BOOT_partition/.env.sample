# BOOT 分区启动环境变量样本（复制为 .env 生效）
# U-Boot 会从 BOOT 分区根目录导入该文件（env import -t）

# 1=启用 PL 首屏链路；0=关闭（默认）
PL_SPLASH_ENABLE=1

# 上电色条停留时长（秒）
PL_COLORBAR_HOLD=2

# 切换 splash 图后的停留时长（秒）
PL_SPLASH_HOLD=1

# 可选：允许 orchestrator 启动时用 BOOT .env 注入 STA 配置
# WIFI_BOOT_FALLBACK_ENABLE=0
